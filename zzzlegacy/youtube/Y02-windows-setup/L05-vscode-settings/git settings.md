# Windows Git Compatibility Settings



Type these commands to make windows github compatible

you may need to run this in an administrator gitbash terminal ...

```
git config --system core.longpaths true
```

```
git config --global core.autocrlf true
```

# Other required git settings

```
git config --global user.name "oxford-developer-tutorials"
git config --global user.email "oxford.developer.tutorials@gmai.com"