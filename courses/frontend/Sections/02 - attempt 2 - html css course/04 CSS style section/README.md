# Lesson 04 — CSS in a style section

In this lesson, you move CSS into a `<style>` block inside the HTML document.

You teach:
- `<style>` inside the page
- basic CSS syntax
- keeping styles together instead of attaching them one by one

This is the first time the code starts to feel organized.
