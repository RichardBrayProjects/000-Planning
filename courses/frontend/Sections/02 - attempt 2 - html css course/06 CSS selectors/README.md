# Lesson 06 — CSS selectors

In this lesson, you show the most important selector types.

You teach:
- tag selectors
- an `id`
- a `class`
- a simple descendant selector

The HTML stays tiny so the student can focus on how the selectors target different elements.
