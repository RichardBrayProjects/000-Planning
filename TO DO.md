First Section Content

1. Introduction
2. Course Repositories
3. Setup mac/windows
4. Setup VS Code
5. Clone the project
6. Project walkthrough
7. Tips on attending this course

To Do

1. Re-record the introduction : new content is in planning repo (both slides and narrative have been updated)

2. Re-record the windows setup lesson at a faster pace and smoother mac-windows transition (slides and narrative have been updated)

3. Record a whole new HTML/CSS section (including new scroller)

4. Push new scroller code to other sections until we abandon them

5. Record last lesson to include new scroller

6. Copy new "last lesson code" to the frontend01-introduction repo so it includes the scroller

7. Record Project walkthrough (using last lesson code above with scrollers) lesson at the end of section 1

8. Record "how to take this course" at end of section 1

- coding along
- video controls

New HTML / CSS section

- introduction - core features
- Vs code, tags (p, h, div), chrome Vs preview
- html semantic tags
- CSS embedded Vs external
- CSS simple selectors - tag, id, class,
- CSS complex selectors
- CSS sizes (pixels Vs em Vs vh/vw)
- CSS borders and radius
- CSS margins and padding,
- CSS block Vs inline, (div, p. h, span)
- CSS overflow and scrolling
- CSS display (fixed, absolute, relative)
- CSS colours
- CSS text (font, size)
- CSS flexbox
- CSS hover
- build simple UI with scrolling
